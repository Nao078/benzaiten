//! 再生位置・曲の長さの表示フォーマット処理。

/// ミリ秒を`mm:ss.cc`（分:秒.百分の一秒）表記に整形する。
pub fn format_time(ms: u64) -> String {
    format!(
        "{:02}:{:02}.{:02}",
        ms / 60_000,
        (ms / 1000) % 60,
        (ms % 1000) / 10
    )
}
